<Window x:Class="Graphic.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Graphic.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition></RowDefinition>
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left">
      <ToolBar Name="MyToolbar" Width="200" Height="30" >
        <Button Background="LightSkyBlue" Content="Open" Name="OpenButton" />
        <Button Background="LightSkyBlue" Content="Close" Name="CloseButton" />
        <Button Background="LightSkyBlue" Content="Exit" Name="ExitButton" />
      </ToolBar>
    </StackPanel>
    
    <ContentControl Grid.Row="1">
      <local:MapView x:Name="map"></local:MapView>
    </ContentControl>


  </Grid>
</Window>
