<Window x:Class="Tracking.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tracking"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
  <DockPanel>
    <Menu DockPanel.Dock="Top">
      <MenuItem Header="_File">
        <MenuItem Header="_New" />
        <MenuItem Header="_Open" />
        <MenuItem Header="_Save" />
        <Separator />
        <MenuItem Header="_Exit" />
      </MenuItem>
      <MenuItem Header="_View">
        <MenuItem Header="_Map" Click="Map_ItemClick" />
        <MenuItem Header="_Camera" Click="Camera_ItemClick" />
        <MenuItem Header="_Chart" Click="Char_ItemClick" />
      </MenuItem>
    </Menu>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition></RowDefinition>
        <RowDefinition></RowDefinition>
        <RowDefinition></RowDefinition>
      </Grid.RowDefinitions>
      <StackPanel prism:RegionManager.RegionName="ContentMap" Grid.Row="0" Background="Green" />
      <StackPanel prism:RegionManager.RegionName="ContentChart" Grid.Row="1"  Background="Red" />
      <StackPanel prism:RegionManager.RegionName="ContentCamera" Grid.Row="2" Background="White" />
    </Grid>
  </DockPanel>

</Window>
